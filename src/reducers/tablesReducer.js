
const initialState = {
    tables: ['Table 1', 'Table 2', 'Table 3', 'Table 4'],
    activeTable: 0

}

export default (state=initialState, action) => {
   switch (action.type) {
       
       
       default : return state
   }
}